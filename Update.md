### Update:
- ubah ukuran tinggi kotak editor pada bagian tag untuk isi kitab
- Jika gw me select text atau click text pada mobile preview, maka akan langsung di arahkan pada bagian datanya yg di editor, seperti fungsi `select an element in the page to inspect it - Ctrl+shift+C` di browser.
- ubah shorcut bold pada editor dengan update: Command `**isi**` akan membuat teks seperti di select (seperti format saat ini, yg terdapat background dengan warna cream yg seolah di select text-nya) dan ubah teks shorcutnya dari `Bold` > `Select`.
- Buat shorcut command baru, yaitu untuk mem bold teks, dengan cara memberi command `<b>isi</b>`. teks yg ada didalam kode itu akan ter bold tanpa ada block dibackround yg seperti di select tersebut.
- Buat shorcut ctrl+s untuk me save data artikel yg di update.
- Apabila data artikel telan disimpan (baik itu dengan klik tombol "Simpan" atau dengan shorcut Ctrl+s), maka focus halamannya tetap menampilkan editor artikelnya (Seperti tidak menutup artikel)!
- Update kata kunci pencarian pada Kitabku dengan kata kunci: daftar bab, daftar artikel, isi penjelasan artikel dan quiz.
- buat shorcut baru dengan teks "Paragraf". Fungsinya, jika di klik, ia akan otomatis mengisi command `&emsp;` pada paragraf baru!
- Buat shorcut baru dengan command `<i>isi</i>` untuk underline (teks miring).


Buat kode lengkap tanpa komentar untuk file yg perlu diupdatenya saja dan pastikan lo tidak mengubah/hapus fungsi/tampilan yg sudah ada dan hanya membuat update sesuai dengan requests permintaan gw diatas